name: Build and Push

on:
  push:
    branches:
      - 'main'
    tags:
      - dev-*
      - stage-*
      - live-*
      - profolio-all-stage-*
      - profolio-all-live-*
  pull_request:
    branches:
      - 'main'

jobs:
  build-and-push:
    uses: SectorLabs/enigma-workflows/.github/workflows/build-and-push.yaml@master
    secrets: inherit
